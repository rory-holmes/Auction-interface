type User = {
    /**
     * User id as defined by the database
     */
    userId: number,
    /**
     * Users username as entered when created
     */
    firstName: string,
    lastName: string,
    email: string,
    password: string,
    image_filename: string

}
