type auction = {
    auctionId: number,
    title: string,
    description: string,
    endDate: string,
    image_filename: string,
    reserve: number,
    sellerId: number,
    sellerFirstName: string,
    sellerLastName: string,
    categoryId: number,
    numBids: number,
    highestBid: number
}